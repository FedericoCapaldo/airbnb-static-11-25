<h1>Flats</h1>


<% @flats.each do |flat| %>
  <div class="flat">
    <h2><%= flat["name"] %></h2>
    <p>💰 <%= flat["price"] %> <%= flat["priceCurrency"] %></p>
    <img src="<%= flat["imageUrl"] %>" alt="<%= flat["name"] %>" style="max-width: 300px; height: auto;">
  </div>
<% end %>
